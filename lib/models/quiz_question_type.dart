class QuizQuestionType {
  static const image = 'image';
  static const listening = 'listening';
  static const text = 'text';

  // backward compatibility (nếu còn json cũ)
  static const audio = 'audio';
}
